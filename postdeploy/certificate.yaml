apiVersion: cert-manager.io/v1 
kind: Certificate
metadata:
  name: appcertificate
  namespace: demoapp
spec:
  secretName: appsecret
  commonName: app.gitopsvote.com
  dnsNames:
  - app.gitopsvote.com
  issuerRef:
    name: appissuer
    kind: Issuer